export function PrototypeBanner() {
  return (
    <div className="bg-amber-400 text-amber-900 text-center text-xs font-bold py-1.5 px-4 tracking-widest uppercase w-full z-50">
      ⚠ PROTOTYPE — Mock Data Only — Not for Production Use ⚠
    </div>
  );
}
